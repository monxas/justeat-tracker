# Home Assistant base URL (no trailing slash)
HA_URL=http://homeassistant.local:8123

# Long-lived access token from HA (Profile → Security → Long-Lived Access Tokens)
# Recommended: create a dedicated user "justeat-tracker" with minimal scope.
HA_TOKEN=eyJxxxxxxx...

# (Optional) Sensor entity ID to push state to. Defaults to sensor.justeat_tracking.
# SENSOR_ID=sensor.justeat_tracking
